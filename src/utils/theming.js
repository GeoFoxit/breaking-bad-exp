import { createTheme } from "@mui/material";

// Default MUI theme overriding
export const DEFAULT_THEME = createTheme({
  palette: {
    mode: "dark",
    primary: {
      main: "#00e271"
    },
  },
});
